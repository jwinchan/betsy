<h2>Order Confirmation | Order #<%= @order.id %></h2>
<h4>Buyer Information</h4>
<section class ="cart-item__container">
  <table class="table">
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Mailing Address</th>
      <th>Credit Card Number</th>
      <th>Credit Card Exp Date</th>
    </tr>
    <tr>
      <td><%= @order.name %></td>
      <td><%= @order.email %></td>
      <td><%= @order.mailing_address %></td>
      <td><%= @order.cc_number %></td>
      <td><%= @order.cc_exp_date %></td>
    </tr>
  </table>
</section>

<section class ="cart-item__container">
  <h4>Order Details</h4>
  <table class="table">
   <thead>
    <tr>
      <th>Product name</th>
      <th>Quantity</th>
      <th>Subtotal</th>
    </tr>
    <thead>
    <tbody>
    <%@order.orderitems.each do |item|%>
    <tr>
      <td><%= item.product.name %></td>
      <td><%= item.quantity %></td>
      <td><%= format_money(item.price) %></td>
    </tr>
    <% end %>
    <tr>
      <td></td>
      <td><h4>Total : </h4></td>
      <td><%=format_money(@order.total) %></td>
    </tr>
    </tbody>
  </table>
</section>

<%= button_to "Back to products", root_path, method: :get, class: "btn btn-primary" %>